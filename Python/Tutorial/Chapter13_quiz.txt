1. What are the main functional differences between a while and a for?

A for iterates through a list and/or something and a while runs until a condition is reached?

2. What’s the difference between break and continue?

A break, as it implies, breaks out of the loop that the code is running through.
The continue statement simply returns to the top of the current loop.

3. When is a loop’s else clause executed?

When the loop is never executed

4. How can you code a counter-based loop in Python?

Either with for and while with a autoincrement value? :|

5. What can a range be used for in a for loop?

To specify a desired number of repetitions