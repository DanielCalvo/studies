Very brief install notes of the Prometheus operator -- installed with defaults so that alloy understands that a ServiceMonitor is

The operator pod is doing nothing since we have no Prometheus for it-s to generate a topic for.

See the parent folder AGENTS.md file for further context 