prometheus:
  monitor:
    enabled: true
    additionalLabels:
      prometheus: opi
      alloy: opi
    relabelings:
      # Supplies the cluster selector expected by the Kubernetes Views dashboards.
      - targetLabel: cluster
        replacement: opi

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 64Mi
