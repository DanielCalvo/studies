prometheus:
  monitor:
    enabled: true
    additionalLabels:
      prometheus: homelab
    relabelings:
      # Supplies the cluster selector expected by the Kubernetes Views dashboards.
      - targetLabel: cluster
        replacement: homelab

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 64Mi
