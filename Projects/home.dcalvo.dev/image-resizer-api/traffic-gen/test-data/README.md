# Generated traffic data

`traffic-gen/run.sh` creates ten JPEGs here with dimensions embedded in their
filenames. The generated images are ignored by Git and can always be recreated
from `generate-test-data.go`.
